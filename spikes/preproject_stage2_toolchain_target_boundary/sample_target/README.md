# sample_target

This is the sample git repository used by `preproject_stage2_toolchain_target_boundary`.

The repository itself is the workflow target:

* source files live here
* `.workflow/` will be installed here
* each run gets a detached worktree under `.workflow/runs/<run-id>/worktree`
