{:namespaces
 {"vibe-flow.core"
  {:layer :product-surface
   :volatility :high
   :module-kind :surface
   :complexity :complex
   :responsibility "Formal product surface for state inspection and recovery-oriented views."
   :split-axis "Split user-facing command routing from runtime bootstrap as the surface grows."}

  "vibe-flow.system"
  {:layer :product-surface
   :volatility :high
   :module-kind :surface
   :complexity :complex
   :responsibility "System wiring for install and system-state entrypoints."
   :split-axis "Split system assembly from lifecycle control when concrete modules arrive."}

  "vibe-flow.workflow.control"
  {:layer :workflow-control
   :volatility :high
   :module-kind :orchestrator
   :complexity :complex
   :responsibility "Minimal workflow orchestration for mgr decisions, run execution, and task persistence."
   :split-axis "Split workflow advance, observe, and recovery flows once the formal control plane lands."}

  "vibe-flow.definition.task-type"
  {:layer :definition-interpretation
   :volatility :high
   :module-kind :interpreter
   :complexity :complex
   :responsibility "Load and resolve installed task_type artifacts from the target."
   :split-axis "Split artifact loading from prepare-run interpretation and hook enforcement as behavior grows."}

  "vibe-flow.management.task-type"
  {:layer :model-management
   :volatility :high
   :module-kind :manager
   :complexity :complex
   :responsibility "Create, register, list, and inspect target-managed task_type packages."
   :split-axis "Split package authoring from registry maintenance and package inspection as task_type lifecycle grows."}

  "vibe-flow.management.domain"
  {:layer :model-management
   :volatility :high
   :module-kind :manager
   :complexity :complex
   :responsibility "Create and validate collection and task domain records with minimal task_type binding."
   :split-axis "Split collection lifecycle from task validation and task persistence coordination as domain behavior grows."}

  "vibe-flow.platform.target.install"
  {:layer :model-management
   :volatility :high
   :module-kind :orchestrator
   :complexity :complex
   :responsibility "Install and reconcile the formal target workflow layout and system models."
   :split-axis "Split bootstrap, layout reconcile, and artifact install when the target substrate becomes concrete."}

  "vibe-flow.platform.support.time"
  {:layer :support-substrate
   :volatility :low
   :module-kind :foundation
   :complexity :simple
   :responsibility "Small time helpers for durable records."
   :stability-role "Time formatting should remain a tiny reusable substrate."}

  "vibe-flow.platform.support.edn"
  {:layer :support-substrate
   :volatility :low
   :module-kind :foundation
   :complexity :simple
   :responsibility "Small EDN read and write helpers."
   :stability-role "EDN IO should remain a stable low-level substrate."}

  "vibe-flow.platform.target.paths"
  {:layer :target-substrate
   :volatility :low
   :module-kind :foundation
   :complexity :simple
   :responsibility "Formal .workflow path and layout helpers."
   :stability-role "Workflow path layout should stay small and stable."}

  "vibe-flow.platform.target.repo"
  {:layer :target-substrate
   :volatility :low
   :module-kind :adapter
   :complexity :simple
   :responsibility "Thin git target inspection helpers."
   :stability-role "Target repo inspection should remain a thin stable adapter."}

  "vibe-flow.platform.state.system-store"
  {:layer :state-persistence
   :volatility :low
   :module-kind :store
   :complexity :simple
   :responsibility "Persistence helpers for install, target, and layout system models."
   :stability-role "System model persistence should stay a thin durable store."}

  "vibe-flow.platform.state.collection-store"
  {:layer :state-persistence
   :volatility :low
   :module-kind :store
   :complexity :simple
   :responsibility "Persistence helpers for collection domain records."
   :stability-role "Collection persistence should remain a thin durable store."}

  "vibe-flow.platform.state.task-store"
  {:layer :state-persistence
   :volatility :low
   :module-kind :store
   :complexity :simple
   :responsibility "Persistence helpers for task domain records."
   :stability-role "Task persistence should remain a thin durable store."}

  "vibe-flow.platform.state.run-store"
  {:layer :state-persistence
   :volatility :low
   :module-kind :store
   :complexity :simple
   :responsibility "Persistence helpers for run runtime records."
   :stability-role "Run persistence should remain a thin durable store."}

  "vibe-flow.platform.state.mgr-run-store"
  {:layer :state-persistence
   :volatility :low
   :module-kind :store
   :complexity :simple
   :responsibility "Persistence helpers for mgr_run runtime records."
   :stability-role "mgr_run persistence should remain a thin durable store."}

  "vibe-flow.platform.runtime.mgr-run"
  {:layer :runtime-integration
   :volatility :high
   :module-kind :adapter
   :complexity :simple
   :responsibility "Prepare and finalize mgr_run runtime records."
   :split-axis "Split prompt materialization from external mgr adapter concerns if mgr runtime support expands."}

  "vibe-flow.platform.runtime.run"
  {:layer :runtime-integration
   :volatility :high
   :module-kind :adapter
   :complexity :complex
   :responsibility "Prepare and finalize worker run runtime records and worktrees."
   :split-axis "Split prepare-run input resolution from finalize-run candidate handling if runtime behavior grows."}

  "vibe-flow.platform.runtime.launcher"
  {:layer :runtime-integration
   :volatility :high
   :module-kind :adapter
   :complexity :complex
   :responsibility "Runtime launcher contract with mock execution and reserved Codex entrypoint."
   :split-axis "Split concrete launcher adapters back out once same-layer runtime contracts are formalized."}

  "vibe-flow.governance.rules"
  {:layer :governance
   :volatility :low
   :module-kind :policy
   :complexity :simple
   :responsibility "Machine-readable governance rule metadata."
   :stability-role "Governance foundation data should stay small and stable."}

  "vibe-flow.governance.manifest"
  {:layer :governance
   :volatility :low
   :module-kind :foundation
   :complexity :simple
   :responsibility "Module manifest loading and lookup helpers."
   :stability-role "Manifest access should remain a stable governance substrate."}

  "vibe-flow.governance.ns-inspect"
  {:layer :governance
   :volatility :low
   :module-kind :foundation
   :complexity :simple
   :responsibility "Namespace/path inspection helpers for governance checks."
   :stability-role "Namespace inspection should remain a stable governance helper layer."}

  "vibe-flow.governance.checks"
  {:layer :governance
   :volatility :high
   :module-kind :manager
   :complexity :complex
   :responsibility "Static governance checks over formal source and test trees."
   :split-axis "Split path/layout checks from module/dependency checks if the governance surface expands."}

  "vibe-flow.governance.cli"
  {:layer :governance
   :volatility :medium
   :module-kind :surface
   :complexity :simple
   :responsibility "Governance command-line entrypoint."
   :stability-role "CLI output can evolve, but command wiring should stay predictable."}

  "vibe-flow.governance.checks-test"
  {:layer :test
   :volatility :medium
   :module-kind :test
   :complexity :simple
   :responsibility "Governance checks regression tests."
   :stability-role "Tests may evolve with governance rules but stay colocated with the checks they validate."}

  "vibe-flow.target.install-test"
  {:layer :test
   :volatility :medium
   :module-kind :test
   :complexity :simple
   :responsibility "Target install and reconcile regression tests."
   :stability-role "Install tests may evolve with the formal layout while staying focused on durable behavior."}

  "vibe-flow.management.task-type-test"
  {:layer :test
   :volatility :medium
   :module-kind :test
   :complexity :simple
   :responsibility "Task type package management regression tests."
   :stability-role "Task type tests may evolve with the package contract while staying focused on installed artifacts."}

  "vibe-flow.management.domain-test"
  {:layer :test
   :volatility :medium
   :module-kind :test
   :complexity :simple
   :responsibility "Collection and task domain model regression tests."
   :stability-role "Domain tests may evolve with the minimal schema while staying focused on persistence and validation."}

  "vibe-flow.workflow.control-test"
  {:layer :test
   :volatility :medium
   :module-kind :test
   :complexity :simple
   :responsibility "Workflow control regression tests for the minimal mock loop."
   :stability-role "Workflow tests may evolve with the minimal control path while staying focused on persisted state transitions."}

  "vibe-flow.runtime.launcher-test"
  {:layer :test
   :volatility :medium
   :module-kind :test
   :complexity :simple
   :responsibility "Runtime launcher regression tests for the real Codex command shape."
   :stability-role "Launcher tests may evolve with runtime integration while staying focused on command construction."}

  "vibe-flow.core-test"
  {:layer :test
   :volatility :medium
   :module-kind :test
   :complexity :simple
   :responsibility "Product surface regression tests for inspect and recovery views."
   :stability-role "Core surface tests may evolve with the inspect surface while staying focused on readable state."}

  "vibe-flow.test-runner"
  {:layer :test
   :volatility :low
   :module-kind :test
   :complexity :simple
   :responsibility "Minimal Clojure test runner entrypoint."
   :stability-role "Test entrypoint should stay thin and stable."}}}
